services:
  - type: web
    name: portfolio-app
    env: node
    buildCommand: npm install && npm run build -- --configuration production
    staticPublishPath: dist/MyPortfolio
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
