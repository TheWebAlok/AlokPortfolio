services:
  - type: web
    name: portfolio-app
    env: node
    buildCommand: npm install && npm run build -- --configuration production
    staticPublishPath: dist/AlokPortfolio
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
